/** @type {import('next').NextConfig} */
const nextConfig = {
  // Set the workspace root to fix the multiple lockfiles warning
  outputFileTracingRoot: "C:\\Users\\soham\\Downloads\\Institute\\institute-app",
  // Disable strict mode for development to reduce unnecessary re-renders
  reactStrictMode: false,
};

export default nextConfig;
